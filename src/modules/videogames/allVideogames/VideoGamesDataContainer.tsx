import React from "react";
import VideoGamesView from "./VideoGamesView";

export default class VideoGamesDataContainer extends React.Component<{}, {}> {

    render(): React.ReactNode {
        return (
            <VideoGamesView/>
        );
    }
}